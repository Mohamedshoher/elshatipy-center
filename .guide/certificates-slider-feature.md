# ميزة عرض شهادات تقدير الطلاب (Student Certificates Slider)

## نظرة عامة
تتيح هذه الميزة عرض الطلاب المتميزين (الحاصلين على أوسمة) في الصفحة الرئيسية للموقع في شكل "شهادات تقدير" متحركة (Slider)، مما يضيف عنصراً جمالياً وتشجيعياً للطلاب.

## كيفية العمل
1. **البيانات الديناميكية**: يعتمد القسم على البيانات الحية للطلاب. يقوم النظام تلقائياً بالبحث عن الطلاب الذين لديهم أوسمة (`badges`).
2. **الترتيب**: يتم ترتيب الطلاب بناءً على تاريخ الحصول على أحدث وسام (الأحدث أولاً)، لضمان ظهور الإنجازات الجديدة دائماً في المقدمة.
3. **التصميم**: يتم عرض كل طالب في "بطاقة شهادة" تحتوي على:
   - اسم الطالب.
   - اسم الوسام الذي حصل عليه.
   - أيقونة الوسام.
   - تاريخ الحصول عليه.
   - تصميم يشبه الشهادة الرسمية مع زخارف وأختام.

## كيفية الاستخدام (للمدير)
1. افتح **إدارة محتوى الصفحة الرئيسية**.
2. انقر على **إضافة قسم**.
3. اختر نوع القسم: **🏅 شهادات تقدير الطلاب**.
4. أدخل عنواناً للقسم (مثل: "لوحة الشرف" أو "فرسان المركز").
5. في الإعدادات الخاصة، يمكنك تحديد **عدد الشهادات للعرض** (الافتراضي 10). هذا يحدد عدد أحدث الطلاب الذين سيظهرون في السلايدر.
6. احفظ القسم.

## الملفات المعدلة/الجديدة
- `components/BadgeCertificatesSlider.tsx`: (ملف جديد) المكون المسؤول عن تصميم وعرض السلايدر والشهادات.
- `types.ts`: إضافة نوع `student_certificates`.
- `components/SectionForm.tsx`: إضافة خيارات الإعداد لهذا القسم في لوحة التحكم.
- `components/Section.tsx`: توجيه نوع القسم الجديد للمكون المناسب.
- `components/LandingPage.tsx`: تمرير بيانات الطلاب للأقسام.

## ملاحظات تقنية
- يتم استثناء الطلاب المؤرشفين (`isArchived`) تلقائياً.
- إذا لم يكن هناك طلاب لديهم أوسمة، لن يظهر القسم (يختفي تلقائياً).
- الانتقال التلقائي بين الشهادات كل 6 ثوانٍ.
