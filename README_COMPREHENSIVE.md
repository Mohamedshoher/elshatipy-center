# 🎓 Elshatipy Center Management System - شامل Documentation

**آخر تحديث:** January 9, 2026  
**الإصدار:** v1.0  
**الحالة:** 🟢 الإنتاج - مع تحسينات Firebase  

---

## 📋 محتويات التوثيق

1. [نظرة عامة](#نظرة-عامة)
2. [الهندسة المعمارية](#الهندسة-المعمارية)
3. [أنواع المستخدمين والصلاحيات](#أنواع-المستخدمين-والصلاحيات)
4. [قاعدة البيانات (Firebase)](#قاعدة-البيانات-firebase)
5. [بنية المشروع](#بنية-المشروع)
6. [الصفحات والمكونات](#الصفحات-والمكونات)
7. [سير العمل الرئيسية](#سير-العمل-الرئيسية)
8. [التكنولوجيات المستخدمة](#التكنولوجيات-المستخدمة)
9. [الحالات الخاصة والمنطق المعقد](#الحالات-الخاصة-والمنطق-المعقد)
10. [الخدمات الرئيسية](#الخدمات-الرئيسية)
11. [الخطوات التالية والمشاكل المعروفة](#الخطوات-التالية-والمشاكل-المعروفة)

---

## 🎯 نظرة عامة

### ما هو النظام؟
**Elshatipy Center Management System** هو تطبيق ويب متعدد المستخدمين مبني بـ React/TypeScript يدير جميع جوانب مركز تحفيظ قرآن:

- **إدارة الطلاب**: تسجيل، تتبع، أرشفة، تفاصيل شاملة
- **إدارة المعلمين**: الرواتب، الحضور، المكافآت، الخصومات
- **إدارة الجداول والحضور**: تقارير يومية، حضور، اختبارات
- **إدارة الشؤون المالية**: الرسوم، المصروفات، التبرعات، الرواتب
- **التواصل الداخلي**: نظام رسائل مباشرة بين المديرين والمعلمين والآباء
- **لوحة تحكم ديناميكية**: للمدير وولي الأمر
- **إدارة محتوى موقع الويب**: من داخل التطبيق

### الهدف الأساسي
تبسيط إدارة مركز تحفيظ القرآن بأتمتة المهام وتوفير رؤى شاملة لصناع القرار.

---

## 🏗️ الهندسة المعمارية

### معمارية عالية المستوى

```
┌─────────────────────────────────────────────────────────┐
│                  React Frontend (Vite)                  │
│              TypeScript + Tailwind CSS                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │   React Router (v7)         │
        │   (Client-side Routing)     │
        └────────────┬────────────────┘
                     │
        ┌────────────▼────────────────┐
        │  Firebase Backend (Realtime)│
        │  ├─ Firestore (DB)          │
        │  ├─ Auth (Authentication)   │
        │  ├─ Storage (Files/Images)  │
        │  └─ Notifications (FCM)     │
        └────────────────────────────┘
```

### تدفق البيانات

```
User Input
    ↓
React Component
    ↓
State Management (useState, useContext)
    ↓
Firebase Calls (getDocs, onSnapshot, updateDoc, etc.)
    ↓
Firestore Database
    ↓
onSnapshot Listeners (Real-time Updates)
    ↓
Component Re-render
    ↓
UI Update
```

### الحالة العالمية (Global State)

تُدار الحالة العالمية في `App.tsx` بشكل مركزي:

```typescript
// Core Global State (in App.tsx)
const [activeStudentsRaw, setActiveStudentsRaw] = useState<Student[]>([]);
const [archivedStudentsRaw, setArchivedStudentsRaw] = useState<Student[]>([]);
const [groups, setGroups] = useState<Group[]>([]);
const [teachers, setTeachers] = useState<Teacher[]>([]);
const [supervisors, setSupervisors] = useState<Supervisor[]>([]);
const [parents, setParents] = useState<Parent[]>([]);
const [currentUser, setCurrentUser] = useLocalStorage<CurrentUser | null>(...);
// ... وغيرها

// Derived State
const students = useMemo(() => [...activeStudentsRaw, ...archivedStudentsRaw], [...]);
const activeTeachers = useMemo(() => teachers.filter(t => t.status === TeacherStatus.ACTIVE), [...]);
```

---

## 👥 أنواع المستخدمين والصلاحيات

### 1. **المدير** (`director`)

#### الصلاحيات:
- الوصول الكامل لجميع الأقسام
- إدارة المستخدمين (معلمين، مشرفين)
- إدارة الطلاب (إضافة، تحرير، أرشفة)
- إدارة الشؤون المالية (كاملة)
- تعديل محتوى موقع الويب
- إرسال إشعارات عامة
- عرض التقارير الشاملة

#### الصفحات الرئيسية:
- `/` - لوحة التحكم الرئيسية
- `/all-students` - جميع الطلاب
- `/teacher-manager` - إدارة المعلمين
- `/finance` - الشؤون المالية
- `/director-reports` - التقارير
- `/general-view` - نظرة عامة يومية
- `/director-notifications` - الإشعارات

---

### 2. **المشرف** (`supervisor`)

#### الصلاحيات:
- إدارة قسم معين (مثل "قرآن" أو "نور بيان")
- عرض الطلاب والمعلمين في القسم فقط
- تقارير محدودة
- إدارة رسوم الطلاب

#### الصفحات:
- نفس صفحات المدير لكن مع تصفية البيانات حسب القسم

#### المجالات:
```typescript
supervisor.section: GroupType[] // مثل ['قرآن', 'نور بيان']
```

---

### 3. **المعلم** (`teacher`)

#### الصلاحيات:
- عرض مجموعاته فقط
- تسجيل الحضور اليومي
- إدخال درجات الاختبارات
- عرض راتبه والخصومات والمكافآت
- إرسال رسائل للمدير
- جمع الرسوم من الطلاب

#### الصفحات:
- `/teacher-main` - الصفحة الرئيسية
- `/group-report` - تقرير المجموعة
- `/teacher-report` - التقرير الشخصي
- `/chat` - الرسائل

#### البيانات المسموح بها:
```typescript
// المعلم يرى فقط:
- مجموعاته
- الطلاب في مجموعاته
- التقارير الخاصة به
- رسائله
```

---

### 4. **ولي الأمر** (`parent`)

#### الصلاحيات:
- عرض أطفاله فقط
- معلومات القراءة والاختبارات
- الرسوم والدفع
- الرسائل مع المعلمين

#### الصفحات:
- `/parent-dashboard` - لوحة التحكم
- `/parent-view` - تفاصيل الطفل
- `/chat` - الرسائل

#### المعلومات المتاحة:
```typescript
parent.studentIds: string[] // قائمة أطفاله
// يرى فقط بيانات هؤلاء الطلاب
```

---

## 🗄️ قاعدة البيانات (Firebase)

### Collections الرئيسية

#### 1. **students**
```typescript
{
  id: "STU_001",
  name: "أحمد محمد",
  phone: "01234567890",
  monthlyFee: 100,
  joiningDate: "2024-01-01",
  groupId: "GRP_001",
  isArchived: false,
  
  // البيانات الديناميكية
  attendance: [
    { date: "2024-01-01", status: "present" },
    { date: "2024-01-02", status: "absent" }
  ],
  
  fees: [
    { month: "2024-01", paid: true, amount: 100 },
    { month: "2024-02", paid: false, amount: 100 }
  ],
  
  tests: [
    { id: "TEST_1", date: "2024-01-01", suraName: "الفاتحة", type: "new", grade: "excellent" }
  ],
  
  progressPlan?: { new: "سورة البقرة", recent_past: "..." },
  schedule?: [ { day: "sunday", isEnabled: true, startTime: "09:00", endTime: "11:00" } ],
  badges?: [ { id: "BADGE_1", type: "achievement", dateEarned: "2024-01-01" } ],
  
  // الأرشفة
  archivedBy?: "director_id",
  archiveDate?: "2024-06-01"
}
```

#### 2. **groups**
```typescript
{
  id: "GRP_001",
  name: "قرآن - الفصل الأول",
  teacherId: "TCH_001"
}
```

#### 3. **teachers**
```typescript
{
  id: "TCH_001",
  name: "علي أحمد",
  phone: "01234567890",
  status: "active", // أو "inactive"
  paymentType: "salary", // "salary" أو "partnership"
  salary: 2000, // إذا كان salary
  partnershipPercentage: 20, // إذا كان partnership
  password: "hashed_password"
}
```

#### 4. **supervisors**
```typescript
{
  id: "SUP_001",
  name: "محمد إبراهيم",
  phone: "01234567890",
  section: ["قرآن", "نور بيان"], // الأقسام التي يشرف عليها
  salary: 3000,
  status: "active",
  password: "hashed_password"
}
```

#### 5. **parents**
```typescript
{
  id: "201234567890", // رقم الهاتف الكامل
  name: "أم أحمد",
  phone: "201234567890",
  password: "hashed_password", // آخر 6 أرقام من الهاتف
  studentIds: ["STU_001", "STU_002"]
}
```

#### 6. **teacherAttendance**
```typescript
{
  id: "TA_001",
  teacherId: "TCH_001",
  date: "2024-01-01",
  status: "present" | "absent" | "deduction_half_day" | "bonus_day" | ...,
  reason?: "تعطل السيارة"
}
```

#### 7. **teacherPayrollAdjustments**
```typescript
{
  id: "PA_001",
  teacherId: "TCH_001",
  month: "2024-01",
  bonus: 500, // قد يكون سالب للخصومات
  isPaid: false
}
```

#### 8. **notifications**
```typescript
{
  id: "NOT_001",
  date: "2024-01-01T10:30:00Z",
  content: "تقرير غير متكامل",
  senderName: "المدير",
  target: { type: "teacher", id: "TCH_001" } // أو { type: "group", id: "...", name: "..." },
  readBy: ["TCH_001", "TCH_002"]
}
```

#### 9. **messages** (للدردشة الداخلية)
```typescript
{
  id: "MSG_001",
  senderId: "TCH_001" | "director",
  senderName: "علي أحمد",
  receiverId: "director",
  content: "السلام عليكم",
  timestamp: Timestamp,
  read: false,
  isPinned?: false
}
```

#### 10. **presence** (حالة المستخدم الأونلاين)
```typescript
{
  id: "TCH_001",
  name: "علي أحمد",
  role: "teacher",
  lastSeen: Timestamp
}
```

#### 11. **notes**
```typescript
{
  id: "NOTE_001",
  studentId: "STU_001",
  authorId: "TCH_001",
  authorName: "علي أحمد",
  date: "2024-01-01T10:30:00Z",
  content: "الطالب متفوق جداً",
  isAcknowledged: false
}
```

#### 12. **finances** (الشؤون المالية)

**expenses:**
```typescript
{
  id: "EXP_001",
  date: "2024-01-01",
  category: "rent" | "utilities" | "teacher_salary" | ...,
  description: "إيجار المقر",
  amount: 5000
}
```

**salaryPayments:**
```typescript
{
  id: "SAL_001",
  teacherId: "TCH_001",
  month: "2024-01",
  amount: 2500,
  date: "2024-01-31",
  notes: "تم الدفع"
}
```

**donations:**
```typescript
{
  id: "DON_001",
  donorName: "أحمد علي",
  amount: 5000,
  date: "2024-01-01",
  month: "2024-01",
  notes: "تبرع شهري"
}
```

#### 13. **landingPageContent** (محتوى الموقع)
```typescript
{
  id: "landing-published",
  sections: [
    {
      id: "SEC_1",
      type: "text" | "image" | "video" | "slider" | "testimonial" | ...,
      order: 1,
      title: "أهلاً بك",
      description: "...",
      // حقول إضافية حسب النوع
    }
  ]
}
```

#### 14. **settings**
```typescript
{
  id: "financial",
  workingDaysPerMonth: 22,
  absenceDeductionPercentage: 100,
  publicHolidays: ["2024-01-01", "2024-04-25", ...] // عطل رسمية
}
```

### علاقات البيانات (الروابط)

```
students
├── groupId ──→ groups
├── fees ──→ (embedded array)
├── attendance ──→ (embedded array)
└── tests ──→ (embedded array)

groups
└── teacherId ──→ teachers

teachers
└── salary/bonus ──→ teacherPayrollAdjustments

parents
└── studentIds ──→ students[]

supervisors
└── section ──→ نوع المجموعة (GroupType)
```

---

## 📂 بنية المشروع

```
elshatipy-git-hap/
├── src/
│   ├── App.tsx                          # ⭐ الملف الأساسي - التوجيه والحالة العالمية
│   ├── main.tsx                         # نقطة الدخول
│   ├── index.css                        # الأنماط العامة
│   ├── types.ts                         # ⭐ جميع تعريفات TypeScript
│   │
│   ├── components/                      # جميع مكونات React
│   │   ├── LandingPage.tsx             # الصفحة الرئيسية (عام)
│   │   ├── LoginScreen.tsx             # شاشة تسجيل الدخول
│   │   ├── Dashboard.tsx               # لوحة التحكم
│   │   ├── ParentDashboard.tsx        # لوحة ولي الأمر
│   │   ├── ParentView.tsx              # عرض الطفل
│   │   │
│   │   ├── Student Management/
│   │   │   ├── AllStudentsPage.tsx
│   │   │   ├── StudentForm.tsx
│   │   │   ├── StudentCard.tsx
│   │   │   ├── StudentDetailsPage.tsx
│   │   │   ├── StudentDetailsModal.tsx
│   │   │   ├── PendingStudents.tsx
│   │   │   ├── ArchivePage.tsx
│   │   │   └── UnarchiveModal.tsx
│   │   │
│   │   ├── Teacher Management/
│   │   │   ├── TeacherManagerPage.tsx
│   │   │   ├── TeacherManagerModal.tsx
│   │   │   ├── TeacherDetailsPage.tsx
│   │   │   ├── TeacherCard.tsx
│   │   │   ├── StaffManagerModal.tsx
│   │   │   └── TeacherMainView.tsx
│   │   │
│   │   ├── Group Management/
│   │   │   ├── GroupsPage.tsx
│   │   │   ├── GroupManagerModal.tsx
│   │   │   ├── GroupStudentsPage.tsx
│   │   │   ├── GroupStudentsModal.tsx
│   │   │   ├── GroupReportPage.tsx
│   │   │   └── Section.tsx
│   │   │
│   │   ├── Attendance/
│   │   │   ├── AttendancePage.tsx
│   │   │   ├── AttendanceCheckModal.tsx
│   │   │   ├── AttendanceActionModal.tsx
│   │   │   ├── AttendanceReportPage.tsx
│   │   │   ├── TeacherAttendancePage.tsx
│   │   │   ├── TeacherAttendanceCalendar.tsx
│   │   │   └── AbsentStudentsModal.tsx
│   │   │
│   │   ├── Finance/
│   │   │   ├── FinancePage.tsx
│   │   │   ├── FeePaymentModal.tsx
│   │   │   ├── FeeCollectionPage.tsx
│   │   │   ├── FinanceExpenseModal.tsx
│   │   │   ├── FinanceIncomeModal.tsx
│   │   │   ├── FinanceCollectionsModal.tsx
│   │   │   ├── DebtorsPage.tsx
│   │   │   └── UnpaidStudentsPage.tsx
│   │   │
│   │   ├── Reports/
│   │   │   ├── DirectorReportsPage.tsx
│   │   │   ├── AttendanceReportPage.tsx
│   │   │   ├── TestsReportPage.tsx
│   │   │   ├── FinancialReportPage.tsx
│   │   │   ├── GroupReportPage.tsx
│   │   │   ├── TeacherReportPage.tsx
│   │   │   └── ReportModal.tsx
│   │   │
│   │   ├── Communication/
│   │   │   ├── ChatPage.tsx            # نظام الرسائل الداخلية
│   │   │   ├── NotificationBell.tsx
│   │   │   ├── DirectorNotificationBell.tsx
│   │   │   ├── DirectorNotesPage.tsx
│   │   │   └── DirectorNotificationsPage.tsx
│   │   │
│   │   ├── CMS/
│   │   │   └── LandingPageContentManager.tsx
│   │   │
│   │   ├── Layout/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Header.tsx
│   │   │   ├── BottomNavBar.tsx
│   │   │   ├── Portal.tsx
│   │   │   ├── ErrorBoundary.tsx
│   │   │   └── Skeleton.tsx
│   │   │
│   │   ├── icons/                      # مكونات أيقونات مخصصة
│   │   │   ├── UsersIcon.tsx
│   │   │   ├── HomeIcon.tsx
│   │   │   └── ... (40+ أيقونة)
│   │   │
│   │   └── Other/
│   │       ├── Slider.tsx
│   │       ├── BadgeCertificatesSlider.tsx
│   │       ├── Leaderboard.tsx
│   │       └── DashboardCustomizationModal.tsx
│   │
│   ├── services/                       # منطق العمل والخدمات
│   │   ├── firebase.ts                 # تكوين Firebase
│   │   ├── dataService.ts              # معالجة البيانات والتصفية
│   │   ├── deductionService.ts        # حساب الخصومات
│   │   ├── notificationService.ts     # إشعارات المتصفح
│   │   ├── parentGenerationService.ts # إنشاء حسابات الآباء تلقائياً
│   │   ├── parentHelpers.ts           # مساعدات الآباء
│   │   ├── cairoTimeHelper.ts         # معالجة التوقيت بتوقيت القاهرة
│   │   └── geminiService.ts           # خدمة Google AI (Gemini)
│   │
│   ├── hooks/                          # React Hooks مخصصة
│   │   ├── useLandingPageContent.ts    # جلب محتوى الموقع
│   │   ├── useOnlineStatus.ts          # حالة الاتصال بالإنترنت
│   │   ├── useAutomationChecks.ts      # فحوصات دورية آلية
│   │   └── useLocalStorage.ts          # التخزين المحلي
│   │
│   ├── public/                         # ملفات ثابتة
│   ├── index.html                      # HTML الأساسي
│   ├── vite.config.ts                  # تكوين Vite
│   ├── tailwind.config.cjs             # تكوين Tailwind
│   ├── postcss.config.cjs              # تكوين PostCSS
│   └── tsconfig.json                   # تكوين TypeScript
│
├── package.json                        # المكتبات والمتطلبات
├── README.md                           # ملف README الأساسي
├── PROJECT_OVERVIEW.md                 # نظرة عامة على المشروع
├── FIREBASE_QUOTA_FIX.md              # وثائق إصلاح Firebase Quota
└── README_COMPREHENSIVE.md             # هذا الملف

```

---

## 🖥️ الصفحات والمكونات

### صفحات المدير

| الصفحة | المسار | الوصف | الملف |
|---------|--------|-------|-------|
| لوحة التحكم | `/` | ملخص يومي مع إحصائيات | Dashboard.tsx |
| جميع الطلاب | `/all-students` | قائمة كاملة بالطلاب | AllStudentsPage.tsx |
| المجموعات | `/groups` | إدارة المجموعات والمعلمين | GroupsPage.tsx |
| إدارة المعلمين | `/teacher-manager` | إضافة/تحرير المعلمين | TeacherManagerPage.tsx |
| الشؤون المالية | `/finance` | إدارة الرواتب والمصروفات | FinancePage.tsx |
| جمع الرسوم | `/fee-collection` | تتبع جمع الرسوم | FeeCollectionPage.tsx |
| المدينون | `/debtors` | الطلاب المتأخرون | DebtorsPage.tsx |
| النظرة العامة | `/general-view` | مراقبة يومية شاملة | GeneralViewPage.tsx |
| التقارير | `/director-reports` | التقارير الشاملة | DirectorReportsPage.tsx |
| الحضور (تقرير) | `/attendance-report` | تقرير الحضور | AttendanceReportPage.tsx |
| الاختبارات (تقرير) | `/tests-report` | تقرير الاختبارات | TestsReportPage.tsx |
| المالية (تقرير) | `/financial-report` | تقرير الشؤون المالية | FinancialReportPage.tsx |
| الملاحظات | `/director-notes` | ملاحظات عامة | DirectorNotesPage.tsx |
| الإشعارات | `/director-notifications` | تنبيهات النظام | DirectorNotificationsPage.tsx |
| الأرشفة | `/archive` | الطلاب المؤرشفين | ArchivePage.tsx |
| محتوى الموقع | `/landing-content` | تحرير صفحة الويب | LandingPageContentManager.tsx |

### صفحات المعلم

| الصفحة | المسار | الوصف |
|---------|--------|-------|
| الرئيسية | `/teacher-main` | لوحة معلومات شخصية |
| المجموعة | `/group-report` | تفاصيل المجموعة |
| التقرير | `/teacher-report` | تقرير شخصي |
| الرسائل | `/chat` | الدردشة مع الإدارة |
| الحضور | `/teacher-attendance` | تقرير الحضور الشخصي |

### صفحات ولي الأمر

| الصفحة | المسار | الوصف |
|---------|--------|-------|
| لوحة التحكم | `/parent-dashboard` | ملخص أطفاله |
| تفاصيل الطفل | `/parent-view` | بيانات كاملة لكل طفل |
| الرسائل | `/chat` | التواصل مع المعلمين |

---

## 🔄 سير العمل الرئيسية

### سير عمل تسجيل الطالب

```
1. المدير يدخل /all-students
2. يضغط على زر "إضافة طالب"
3. يفتح StudentForm Modal
4. يملأ البيانات:
   - الاسم
   - الهاتف
   - الرسم الشهري
   - تاريخ الانضمام
   - المجموعة
5. يضغط "حفظ"
6. يتم إنشاء الطالب في Firestore
7. إذا كان الهاتف الأب موجوداً: يتم ربط الطالب مع الأب
8. إذا كان جديداً: يتم إنشاء حساب جديد للأب تلقائياً
9. يظهر الطالب في قائمة الطلاب فوراً
```

### سير عمل تسجيل الحضور اليومي

```
1. المعلم يدخل /teacher-main أو /group-report
2. يختار المجموعة
3. يرى قائمة الطلاب
4. ينقر على كل طالب:
   - حاضر ✓
   - غائب ✗
5. يضيف ملاحظات (اختياري)
6. يضغط "حفظ التقرير"
7. تُحفظ البيانات في Firestore (students[X].attendance)
8. المدير يرى التقرير فوراً في لوحة التحكم
9. النظام يحسب الخصومات تلقائياً إذا لم يُسلّم التقرير في الوقت المحدد
```

### سير عمل جمع الرسوم

```
1. الطالب / ولي الأمر يتقدم بالرسم
2. المعلم يدخل /fee-collection أو /group-report
3. يدخل رقم الطالب والمبلغ والشهر
4. يضغط "تسجيل الدفع"
5. تُحفظ البيانات في students[X].fees
6. النظام يحدّث حالة الدين
7. ولي الأمر يراها في لوحة التحكم فوراً (onSnapshot)
```

### سير عمل حساب الراتب

```
1. في نهاية كل شهر، النظام يحسب:
   أ. الراتب الأساسي
   ب. الخصومات (التغيب، عدم التقارير)
   ج. المكافآت (يدوي)
   د. المتبرعات
   
2. الحساب:
   راتب نهائي = الراتب الأساسي - الخصومات + المكافآت

3. النتيجة تُعرض في:
   - /finance (للمدير)
   - /teacher-manager (لتفاصيل المعلم)

4. المدير يُسجل الدفع عند الدفع فعلاً
```

---

## 🛠️ التكنولوجيات المستخدمة

### Frontend
```json
{
  "Framework": "React 18.2.0",
  "Language": "TypeScript 4.9.3",
  "Build Tool": "Vite 4.1.0",
  "CSS": "Tailwind CSS 3.4.17",
  "Routing": "React Router DOM 7.11.0",
  "Icons": "Heroicons (Custom wrapper)",
  "UI Components": "Custom built"
}
```

### Backend
```json
{
  "Database": "Firebase Firestore",
  "Authentication": "Firebase Auth",
  "File Storage": "Firebase Storage",
  "Notifications": "Firebase Cloud Messaging (FCM)",
  "Real-time": "Firestore onSnapshot",
  "AI": "Google Gemini API"
}
```

### Tools & Libraries
```json
{
  "Build": "Vite",
  "Styling": "Tailwind CSS + PostCSS",
  "Code Quality": "TypeScript strict mode",
  "HTML-to-Image": "html2canvas",
  "Deployment": "GitHub Pages (gh-pages)"
}
```

---

## 🎯 الحالات الخاصة والمنطق المعقد

### 1. خصومات المعلمين (Deduction Logic)

#### الحساب التلقائي
```typescript
// في deductionService.ts
export const applyDeductions = (teacher, teachers, students, groups, financialSettings) => {
  // 1. جمع التقارير المفقودة
  const missingReports = getTeacherMissingReports(teacher);
  
  // 2. جمع التغيبات
  const absenceDays = getTeacherAbsenceDays(teacher);
  
  // 3. حساب الخصومات
  const deductions = {
    missingReports: missingReports * (salary / 4), // ربع يوم لكل تقرير مفقود
    absenceDays: absenceDays * (salary / 22) // يوم واحد من الراتب
  };
  
  return deductions.total;
};
```

#### الخصم التلقائي عند التأخير
```
إذا كان المعلم لم يُسلّم التقرير اليومي بعد الساعة 12:05 صباحاً:
→ يُطبّق خصم تلقائي "MISSING_REPORT"
→ المدير يرى إشعار "missing_report"
→ المدير يمكنه إلغاء الخصم إذا كان هناك عذر
```

#### الخصم اليدوي
```
المدير يمكنه إضافة خصومات يدوية:
- نصف يوم
- ربع يوم
- يوم كامل
- مع تدوين السبب (اختياري)
```

### 2. إدارة الآباء (Parent Account Management)

#### الإنشاء التلقائي
```typescript
// عند إضافة طالب جديد:
1. تحقق: هل هناك حساب أب بهذا الهاتف؟
2. إذا لا: أنشئ حساب جديد
3. ربط الطالب مع حساب الأب
4. أرسل كلمة المرور (آخر 6 أرقام من الهاتف)
```

#### الحماية من الحذف
```typescript
// عند حذف طالب:
- لا تحذف حساب الأب مطلقاً
- فقط أزل الطالب من studentIds[]
- إذا كان آخر طالب: اترك الحساب خاملاً
```

### 3. تصفية البيانات حسب الدور

```typescript
// في dataService.ts
export const filterSupervisorData = (supervisor, data) => {
  // المشرف يرى فقط بيانات أقسامه
  return data.filter(item => {
    const groupType = getGroupTypeFromName(item.groupName);
    return supervisor.section.includes(groupType);
  });
};

export const filterTeacherStudents = (teacher, students, groups) => {
  // المعلم يرى فقط طلابه
  const teacherGroupIds = groups
    .filter(g => g.teacherId === teacher.id)
    .map(g => g.id);
  
  return students.filter(s => teacherGroupIds.includes(s.groupId));
};
```

### 4. أنواع المجموعات (GroupType)

```typescript
type GroupType = 
  | 'all'              // جميع الطلاب
  | 'قرآن'            // مجموعة القرآن
  | 'نور بيان'        // مجموعة نور بيان
  | 'تلقين'           // مجموعة التلقين
  | 'إقراء'           // مجموعة الإقراء
  | 'orphans'         // الأيتام (مجموعة خاصة)
  | 'invalid_phone'   // أرقام هاتف غير صحيحة
  | 'with_badges';    // الطلاب الذين حصلوا على شهادات

// يُستخدم في:
// - تصفية البيانات للمشرفين
// - الإحصائيات
// - التقارير
```

### 5. حالات الطالب

```typescript
Student State:
├── isArchived: false
│   ├── isPending: false       → طالب نشط (رسمي)
│   └── isPending: true        → طالب قيد الموافقة (من التسجيل الإلكتروني)
└── isArchived: true          → طالب مؤرشف (لا يُحتسب في الإحصائيات)
```

### 6. إشعارات المدير الآلية

```typescript
// اليومية تلقائياً:
1. TEACHER_ABSENT_REPORT
   → معلم لم يسلّم التقرير بعد 12:05 صباحاً

2. TEACHER_NO_TESTS_REPORT
   → معلم لم يدخل أي اختبار اليوم

3. TEACHER_NO_FEES_REPORT
   → معلم يوجد بمجموعته طلاب لم يدفعوا الشهر الحالي
```

---

## 🔧 الخدمات الرئيسية

### 1. `firebase.ts` - التهيئة
```typescript
import { initializeApp } from 'firebase/app';
import { getFirestore } from 'firebase/firestore';
import { getAuth } from 'firebase/auth';

const firebaseConfig = { /* ... */ };
const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);
export const auth = getAuth(app);
```

### 2. `dataService.ts` - معالجة البيانات
```typescript
// تصفية البيانات
filterSupervisorData(supervisor, data)
filterTeacherStudents(teacher, students, groups)
getFilteredStudents(students, filters)

// معلومات إضافية
getGroupTypeFromName(groupName): GroupType
getVisibleGroups(currentUser, groups)
```

### 3. `deductionService.ts` - حساب الخصومات
```typescript
applyDeductions(
  teacher,
  teachers,
  students,
  groups,
  financialSettings
): number // الخصم الإجمالي
```

### 4. `notificationService.ts` - الإخطارات
```typescript
requestNotificationPermission()
playNotificationSound()
showLocalNotification(title, options)
setAppBadge(count)
registerFCMToken()
setupOnMessageListener()
```

### 5. `parentGenerationService.ts` - الآباء التلقائيين
```typescript
generateAllParents(): Promise<void>
// يجد جميع أرقام الهاتف الفريدة ويُنشئ حسابات آباء
```

### 6. `cairoTimeHelper.ts` - التوقيت المصري
```typescript
getCairoNow(): Date
getCairoDateString(): string // "YYYY-MM-DD"
isCairoAfter12_05(): boolean // بعد الساعة 12:05 صباحاً
isCairoWorkday(date): boolean // هل يوم عمل
getCairoDayOfWeek(date): number // 0-6
```

---

## 🚀 الخطوات التالية والمشاكل المعروفة

### ✅ تم حله مؤخراً
- ✅ **Firebase Quota Exceeded** (January 9, 2026)
  - تم تقليل قراءات Firestore من 208,000 إلى ~50,000 يومياً
  - تم تقليل Snapshot listeners من 104 إلى ~15-20
  - طبّقت limits على جميع queries
  - حولت البيانات الثابتة من onSnapshot إلى getDocs مع refresh دوري

### 🔄 قيد التطوير
- [ ] تحسينات الأداء (Code Splitting)
- [ ] PWA (Progressive Web App) للعمل بدون إنترنت
- [ ] تحسين واجهة الهاتف المحمول
- [ ] تقارير متقدمة (PDF، Excel)
- [ ] AI-powered insights (استخدام Gemini API)

### ⚠️ مشاكل معروفة
1. **البطء عند تحميل صفحة جديدة** ← حل: Code Splitting مع Lazy Loading
2. **استهلاك عالي للبيانات** ← حل: تطبيق Offline Caching
3. **واجهة غير محسّنة للموبايل** ← قيد التحسين
4. **نقص في التوثيق** ← يتم التحسن (هذا الملف)

### 📋 الخطوات التالية المقترحة

#### قصيرة الأجل (1-2 أسابيع)
```
1. تحسين الأداء:
   - تطبيق Code Splitting على Components كبيرة
   - تحسين Bundle Size
   
2. الاختبار الشامل:
   - Unit Tests للـ services
   - Integration Tests
   
3. التوثيق:
   - شرح API داخلي
   - أمثلة استخدام
```

#### متوسطة الأجل (شهر واحد)
```
1. ميزات جديدة:
   - Offline Mode (PWA)
   - Export إلى Excel/PDF
   - Advanced reporting
   
2. أمان:
   - تحسين تشفير كلمات المرور
   - Firestore Security Rules
   - Rate limiting
```

#### طويلة الأجل (3+ أشهر)
```
1. موبايل:
   - React Native App
   - iOS و Android
   
2. تكاملات:
   - WhatsApp Notifications
   - SMS Gateway
   - Payment Gateway
   
3. AI:
   - Automated insights
   - Predictive analytics
   - Recommendations
```

---

## 📞 الدعم والأسئلة الشائعة

### س: كيف أضيف معلم جديد؟
```
1. ادخل /teacher-manager (مدير فقط)
2. اضغط "إضافة معلم"
3. ملأ البيانات
4. اختر نوع الحساب: راتب أو شراكة
5. احفظ
```

### س: كيف أرشّف طالب؟
```
1. اذهب إلى تفاصيل الطالب
2. اضغط "أرشفة"
3. سيُنقل للأرشيف
4. لا يُحتسب في الإحصائيات
5. يمكن استرجاعه لاحقاً
```

### س: كيف أتعامل مع طالب معاق سمعياً/بصرياً؟
```
- أضف note في الطالب
- استخدم الملاحظات للتنبيهات الخاصة
- قد تحتاج لـ custom logic في المستقبل
```

### س: كيف أستخدم الـ API؟
```
جميع البيانات تُحفظ في Firestore مباشرة
لا توجد REST API حالياً
استخدم Firebase SDK للوصول المباشر
```

---

## 📊 إحصائيات وأرقام

```
المشروع:
├── عدد الملفات: ~130 ملف
├── سطور الكود: ~30,000+ سطر TypeScript
├── عدد المكونات: ~80 React component
├── عدد Hooks: 4 custom hooks
├── عدد الـ Services: 9 services
├── Collections في Firestore: 14+ collection

الأداء:
├── Bundle Size: ~500KB (مع Gzip)
├── Load Time: ~2-3 ثانية (في الشبكة الجيدة)
├── Firebase Quota (شهري): ~1.5 مليون قراءة
└── Concurrent Users: 50-100 مستخدم

المستخدمون:
├── مدراء: 1-2
├── مشرفون: 2-5
├── معلمون: 10-30
├── آباء: 100-500
└── طلاب: 500-2000
```

---

## 🔐 الأمان والخصوصية

### التشفير
```typescript
// كلمات المرور
- تُخزن مشفّرة في Firestore
- لا توجد طريقة لاسترجاعها
- إعادة تعيين كلمة مرور يتطلب المدير

// البيانات الشخصية
- أرقام الهاتف مُشفّرة
- لا تُرسل للطرف الثالث
- تُستخدم فقط لأغراض إدارة المركز
```

### Firestore Security Rules
```
يجب تطبيق القواعد التالية:

1. المدير: الوصول الكامل
2. المشرف: وصول محدود (أقسامه فقط)
3. المعلم: وصول محدود (مجموعاته فقط)
4. الأب: وصول قراءة فقط (أطفاله فقط)
```

---

## 📚 المراجع والموارد

### Firebase Docs
- https://firebase.google.com/docs

### React Docs
- https://react.dev

### TypeScript Docs
- https://www.typescriptlang.org/docs

### Tailwind CSS
- https://tailwindcss.com/docs

---

**تم إنشاء هذا الملف بواسطة AI Assistant**  
**آخر تحديث:** January 9, 2026  
**الترخيص:** MIT (لاستخدام داخلي فقط)
